# ADR-001 백엔드 도메인 구조

## 결정
- 백엔드는 `auth`, `material`, `question`, `submission`, `dashboard`, `qa`, `common` 최상위 도메인으로 분리합니다.

## 이유
- 기능 경계를 명확히 하고 도메인 간 직접 간섭을 줄이기 위함입니다.

## 대안
- 계층 우선 패키지 구조도 가능하지만, 현재 요구는 도메인 최상위 분리이므로 채택하지 않습니다.