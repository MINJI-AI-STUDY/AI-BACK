# 멀티테넌시 아키텍처

## 기준
- `schoolId`를 최상위 tenant 경계로 사용
- `classroomId`를 학교 하위 조직 식별자로 사용

## 원칙
- 모든 도메인 데이터는 최소 `schoolId`를 보유
- 학급 맥락이 필요한 데이터는 `classroomId` 추가
- 인증 컨텍스트에서 tenant를 복원하고 서비스 계층에서 강제

## 상세 결정
- `../00_기준/ADR/ADR-004_학교-학급_논리_멀티테넌시.md` 참조
